extend layout
block content
  div
    | Please enter connector details
    div
    if listHistorySchedulers
      each scheduler, key in listHistorySchedulers
        table
          thead
            tr
              th(width="200")
                |Name
              th
              th
                |Description
              th
              th(width="150")
              th
              th(width="150")
                |Status
              th
            tr
          thead
          tbody
            tr
              td #{ scheduler.name }
              td
              td #{ scheduler.name }
              td
              td
              td
              td
                div(class="switch large switch-scheduler")
                  input(class="switch-input input-scheduler" id="yes-no#{ scheduler.idDevice }" type="checkbox" name="schedulerSwitch" data-urlcall="#{urlToCallScheduler}/#{ scheduler.idDevice }/"
                  checked=scheduler.running == true)
                  label(class="switch-paddle" for="yes-no#{ scheduler.idDevice }")
                    span(class="switch-active" aria-hidden="true")
                      |On
                    span
                    span(class="switch-inactive" aria-hidden="true")
                      |Off
                    span
                  label
                div
              td
            tir
          tbody
        table
        hr
block sidebar
  .widget
    p
